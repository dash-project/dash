#
# In-place makefile for use side-by-side with the 
# CMake build system
#

# where the DART interface is located
DART_IF = ../../dart-if/v3

CC      = gcc -std=c99 $(OPT_FLAGS)
CXX     = g++ -std=c++0x $(OPT_FLAGS)
#CXX     = clang -std=c++11 $(OPT_FLAGS)

LIBDART_EXTRA = -lpthread
