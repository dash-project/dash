coverage:
  precision: 2
  round: down
  range: "70...100"
  status:
    project:
      default:
        threshold: 0.1
    patch:
      default:
        target: 50

comment:
  layout: "header, diff, changes, uncovered, tree"
  behavior: default
