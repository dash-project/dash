Build instructions:

1. Set shell environment:  DASH_INCLUDES, DASH_LIBS
2. cp gtest headers and libs to $DASH_INCLUDES/gtest and $DASH_LIBS respectively to build tests.
3. Call make in the dash root directory.
