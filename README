Build instructions:

1. Set shell environment:  DASH_INCLUDES, DASH_LIBS
2. create gtest-lib (unzip gtest-1.6.0.zip -> cd gtest-1.6.0 -> ./configure -> make)
3. cp include/gtest $DASH_INCLUDES 
4. cp lib/.libs/libgtest.a $DASH_LIBS
5. Call make in the dash root directory.

or just use the script:

> . set_env.sh path_to_include_lib_parent_dir
