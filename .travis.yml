sudo: required
dist: trusty
os:   linux
services:
    - docker
language: cpp
compiler:
    - gcc
before_install:
    - docker build --tag=dash/testing ./dash/scripts/docker-testing/
script:
    - docker run -d -v $PWD:/opt/dash dash/testing /bin/sh -c "cd /opt/dash; sh dash/scripts/dash-ci.sh;"
after_failure:
    - cat ./build-ci/*/*/*.log
