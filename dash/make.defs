# change this to the name of the DART implementation 
# ("mpi", "shmem", etc.)
DART_IMPL   = shmem

LIBDART_DIR = $(DASH_ROOT)/../dart-impl/$(DART_IMPL)/dart-$(DART_IMPL)/

include $(LIBDART_DIR)/../make.defs

LIBDART     = $(LIBDART_DIR)/libdart.a 
DART_INC    = $(LIBDART_DIR)/../$(DART_IF)

